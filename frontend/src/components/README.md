# Components

This directory contains React components for the Fast Food Ordering System.

## Structure

- `Menu/` - Menu display and item selection components
- `Order/` - Order management and cart components
- `Checkout/` - Checkout and payment components
- `Common/` - Shared/reusable components (buttons, inputs, etc.)

